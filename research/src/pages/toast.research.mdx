---
name: Toast
path: /components/toast.research
menu: Analysis
---

A toast is an element that displays a brief, non-critical message in a way that attracts the user's attention without interrupting the user's task. Toasts are often displayed in the corner of app UI, and often disappear on a timeout.

import ComponentCoverage from '../components/component-coverage'

<ComponentCoverage component="Toast" />

import Concepts from '../components/concepts'

## Concepts

<Concepts component="Toast" />
