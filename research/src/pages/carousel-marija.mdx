---
menu: Components
name: Carousel (Marija)
path: /components/carousel-marija
---

import _ from 'lodash'
import Anatomy from '../components/anatomy'
import AnatomyGrid from '../components/anatomy-grid'
import Concepts from '../components/concepts'
import ComponentCoverage from '../components/component-coverage'
import ConceptCoverage from '../components/concept-coverage'
import Image from '../components/image'
import { conceptsByComponent, getImagesForComponent } from '../sources'

# Carousel

<ComponentCoverage component="Carousel" />

## Anatomy

<AnatomyGrid
  gridTemplate={`
    ".    slide    slide  slide  slide  .   " 80px
    "prev slide    slide  slide  slide  next" 60px
    ".    slide    slide  slide  slide  .   " 80px
    ".    rotation picker picker picker .   " 60px /
     60px 60px     1fr    1fr    80px   60px
  `}
/>

- slide - <small style={{ opacity: 0.75 }}>A single content container within a set of content containers that hold the content to be presented by the carousel.</small>
- rotation control - <small style={{ opacity: 0.75 }}>An interactive element that stops and starts automatic slide rotation.</small>
- slide picker controls - <small style={{ opacity: 0.75 }}>A group of elements, often styled as small dots, that enable the user to pick a specific slide in the rotation sequence to display.</small>
- previous slide control - <small style={{ opacity: 0.75 }}>An interactive element, often styled as an arrow, that displays the previous slide in the rotation sequence.</small>
- next slide control - <small style={{ opacity: 0.75 }}>An interactive element, often styled as an arrow, that displays the next slide in the rotation sequence.</small>

## Concepts

<ul>
  {_.map(conceptsByComponent['Carousel'], (concept, conceptOpenUIName) => (
    <li key={conceptOpenUIName}>
      {conceptOpenUIName} <ConceptCoverage component="Carousel" concept={conceptOpenUIName} />
      {_.map(concept, item => (
        <>
          <br />
          <small>
            {item.sourceName}
            <b>[{item.name}]</b>
          </small>
          <small style={{ opacity: 0.75 }}>
            {': '}
            {item.description}
          </small>
        </>
      ))}
    </li>
  ))}
</ul>

## Specimens

<div>
  {_.map(getImagesForComponent('Carousel'), (src, i) => (
    <Image key={i} src={src} title={src} />
  ))}
</div>
